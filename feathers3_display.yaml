esphome:
  name: entre-display
  friendly_name: Entre Display
  on_boot:
    - priority: 600      # LDO2 tændes tidligst muligt
      then:
        - output.turn_on: ldo2_enable
    - priority: -100     # Kør script efter alle komponenter (inkl. WiFi) er initialiseret
      then:
        - script.execute: update_and_sleep

esp32:
  board: um_feathers3
  variant: esp32s3
  framework:
    type: esp-idf

logger:
  level: DEBUG
  hardware_uart: USB_CDC
  logs:
    sensor: DEBUG
    text_sensor: DEBUG
    display: DEBUG
    wifi: DEBUG

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  fast_connect: true
  on_connect:
    - logger.log: "WiFi forbundet!"
  on_disconnect:
    - logger.log: "WiFi mistet forbindelsen"

api:
  encryption:
    key: !secret api_encryption_key

ota:
  - platform: esphome

# ---------- Tid via Home Assistant ----------
time:
  - platform: homeassistant
    id: sntp_time
    timezone: Europe/Copenhagen

# ---------- Deep Sleep ----------
deep_sleep:
  id: deep_sleep_control
  sleep_duration: 10min
  wakeup_pin:
    number: GPIO0
    inverted: true

# ---------- Script: vent på WiFi + data, opdater display, sov ----------
script:
  - id: update_and_sleep
    then:
      - deep_sleep.prevent: deep_sleep_control
      - wait_until:
          condition:
            wifi.connected:
          timeout: 45s
      - logger.log: "WiFi OK – venter på HA data..."
      - wait_until:
          condition:
            lambda: 'return !isnan(id(outdoor_temp).state);'
          timeout: 30s
      - logger.log: "Opdaterer display"
      - component.update: epaper
      - delay: 10s        # Waveshare 4.2" bruger 4-6s på fuldt refresh
      - output.turn_off: ldo2_enable
      - deep_sleep.enter: deep_sleep_control

# ---------- LDO2 enable (GPIO39 → 3V3_SW → display VCC) ----------
output:
  - platform: gpio
    pin: GPIO39
    id: ldo2_enable

# ---------- Battery (sensor til HA, vises ikke på display) ----------
sensor:
  - platform: adc
    pin: GPIO2
    id: battery_voltage
    name: "Battery Voltage"
    update_interval: 30s
    attenuation: 12db
    filters:
      - multiply: 2.0
    unit_of_measurement: "V"
    accuracy_decimals: 2

  - platform: homeassistant
    id: outdoor_temp
    entity_id: sensor.dantherm_outdoor_temperature
    internal: true

  - platform: homeassistant
    id: indoor_temp
    entity_id: sensor.dantherm_extract_temperature
    internal: true

# ---------- Text sensors ----------
text_sensor:
  # Vejr
  - platform: homeassistant
    id: weather_state
    entity_id: weather.forecast_home
    internal: true

  # Kalender-events – præformateret og sorteret fra HA automation
  - platform: homeassistant
    id: cal_event_1
    entity_id: input_text.display_event_1
    internal: true
  - platform: homeassistant
    id: cal_event_2
    entity_id: input_text.display_event_2
    internal: true
  - platform: homeassistant
    id: cal_event_3
    entity_id: input_text.display_event_3
    internal: true
  - platform: homeassistant
    id: cal_event_4
    entity_id: input_text.display_event_4
    internal: true
  - platform: homeassistant
    id: cal_event_5
    entity_id: input_text.display_event_5
    internal: true
  - platform: homeassistant
    id: cal_event_6
    entity_id: input_text.display_event_6
    internal: true
  - platform: homeassistant
    id: cal_event_7
    entity_id: input_text.display_event_7
    internal: true
  - platform: homeassistant
    id: cal_event_8
    entity_id: input_text.display_event_8
    internal: true
  - platform: homeassistant
    id: cal_event_9
    entity_id: input_text.display_event_9
    internal: true
  - platform: homeassistant
    id: cal_event_10
    entity_id: input_text.display_event_10
    internal: true

  # Vejr-note fra HA forecast ("Regn fra kl. 14")
  - platform: homeassistant
    id: vejr_note
    entity_id: input_text.display_vejr_note
    internal: true

  # Alarm
  - platform: homeassistant
    id: alarm_status
    entity_id: alarm_control_panel.home
    internal: true

# ---------- Fonts ----------
font:
  - file: "fonts/Roboto-Bold.ttf"
    id: font_large
    size: 24
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÆæØøÅå0123456789 .,:;-/°%!?–"
  - file: "fonts/Roboto-Regular.ttf"
    id: font_medium
    size: 18
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÆæØøÅå0123456789 .,:;-/°%!•?–()"
  - file: "fonts/Roboto-Regular.ttf"
    id: font_small
    size: 14
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÆæØøÅå0123456789 .,:;-/°%!•?–()"

# ---------- SPI ----------
spi:
  clk_pin: GPIO12
  mosi_pin: GPIO11

# ---------- ePaper Display ----------
display:
  - platform: waveshare_epaper
    id: epaper
    cs_pin: GPIO10
    dc_pin: GPIO5
    busy_pin: GPIO6
    reset_pin: GPIO14
    model: 4.20in
    update_interval: never
    rotation: 0°
    lambda: |-
      const int W = 400;
      const int H = 300;
      const int M = 8;  // margin

      Color BG = COLOR_OFF;  // hvid baggrund
      Color FG = COLOR_ON;   // sort tekst

      it.fill(BG);
      auto now = id(sntp_time).now();

      // ── Dato + tid ────────────────────────────────────
      int y = M;
      if (now.is_valid()) {
        const char* ugedage[] = {
          "Søndag","Mandag","Tirsdag","Onsdag","Torsdag","Fredag","Lørdag"
        };
        const char* maaneder[] = {
          "","jan","feb","mar","apr","maj","jun","jul","aug","sep","okt","nov","dec"
        };
        char dato[32];
        snprintf(dato, sizeof(dato), "%s %d. %s %d",
          ugedage[now.day_of_week], now.day_of_month,
          maaneder[now.month], now.year);
        it.print(M, y, id(font_large), FG, dato);

        char tid[8];
        snprintf(tid, sizeof(tid), "%02d:%02d", now.hour, now.minute);
        it.print(W - M, y, id(font_large), FG, TextAlign::TOP_RIGHT, tid);
      } else {
        it.print(M, y, id(font_large), FG, "Ingen tid");
      }

      y += 32;
      it.line(M, y, W - M, y, FG);
      y += 6;

      // ── Vejr + temperaturer ───────────────────────────
      std::string ws = id(weather_state).state;
      const char* vejr =
        ws == "sunny"            ? "Solskin" :
        ws == "clear-night"      ? "Klar nat" :
        ws == "partlycloudy"     ? "Lidt skyer" :
        ws == "cloudy"           ? "Overskyet" :
        ws == "rainy"            ? "Regnvejr" :
        ws == "pouring"          ? "Kraftigt regn" :
        ws == "snowy"            ? "Snevejr" :
        ws == "snowy-rainy"      ? "Slud" :
        ws == "windy"            ? "Blæsevejr" :
        ws == "windy-variant"    ? "Blæsevejr" :
        ws == "fog"              ? "Tåget" :
        ws == "hail"             ? "Haglvejr" :
        ws == "lightning"        ? "Tordenvejr" :
        ws == "lightning-rainy"  ? "Torden og regn" :
        ws == "exceptional"      ? "Usædvanligt" : "?";

      float ude  = id(outdoor_temp).state;
      float inde = id(indoor_temp).state;

      // Vejr + udetemperatur på én linje
      char vejr_linje[48];
      if (!isnan(ude))
        snprintf(vejr_linje, sizeof(vejr_linje), "%s, %.1f°", vejr, ude);
      else
        snprintf(vejr_linje, sizeof(vejr_linje), "%s", vejr);
      it.print(M, y, id(font_medium), FG, vejr_linje);

      // Indendørs temp højrejusteret (skjul hvis 0 eller unavailable)
      if (!isnan(inde) && inde != 0.0f) {
        char inde_str[16];
        snprintf(inde_str, sizeof(inde_str), "Inde %.1f°", inde);
        it.print(W - M, y, id(font_medium), FG, TextAlign::TOP_RIGHT, inde_str);
      }

      y += 24;

      // Vejr-note fra HA forecast ("Regn fra kl. 14")
      std::string vnote = id(vejr_note).state;
      if (!vnote.empty() && vnote != "unavailable" && vnote != "unknown") {
        it.print(M, y, id(font_small), FG, vnote.c_str());
        y += 18;
      }

      y += 4;
      it.line(M, y, W - M, y, FG);
      y += 6;

      // ── Kalender (præformateret fra HA) ──────────────
      it.print(M, y, id(font_medium), FG, "KALENDER");
      y += 22;

      const std::string cal_events[] = {
        id(cal_event_1).state,  id(cal_event_2).state,
        id(cal_event_3).state,  id(cal_event_4).state,
        id(cal_event_5).state,  id(cal_event_6).state,
        id(cal_event_7).state,  id(cal_event_8).state,
        id(cal_event_9).state,  id(cal_event_10).state,
      };
      int vis = 0;
      for (int i = 0; i < 10 && y < H - 28; i++) {
        if (cal_events[i].empty() || cal_events[i] == "unavailable") continue;
        char linje[64];
        snprintf(linje, sizeof(linje), "• %.60s", cal_events[i].c_str());
        it.print(M, y, id(font_small), FG, linje);
        y += 17;
        vis++;
      }
      if (vis == 0) {
        it.print(M, y, id(font_small), FG, "Ingen kommende events");
        y += 17;
      }

      y += 4;
      it.line(M, y, W - M, y, FG);
      y += 6;

      // ── Alarm ─────────────────────────────────────────
      std::string alarm = id(alarm_status).state;
      const char* alarm_txt =
        (alarm == "armed_away" || alarm == "armed_home") ? "Alarm: ARMET" :
        alarm == "disarmed"  ? "Alarm: FRA" :
        alarm == "triggered" ? "Alarm: UDLOEST" : "Alarm: ?";
      it.print(M, y, id(font_small), FG, alarm_txt);

      // ── Ramme ─────────────────────────────────────────
      it.rectangle(0, 0, W, H, FG);
