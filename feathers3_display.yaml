# ┌─────────────────────────────────────────────────────────────────────────────┐
# │  USER CONFIGURATION — edit these before flashing                           │
# │  Credentials (WiFi, API key, HA URL/token) go in secrets.yaml             │
# └─────────────────────────────────────────────────────────────────────────────┘

substitutions:
  # GPIO pin assignments
  # FeatherS3 defaults shown. See README for ESP32-S3-WROOM-1 N16R8 pin guide.
  pin_spi_mosi:    "GPIO11"   # ePaper DIN
  pin_spi_sck:     "GPIO12"   # ePaper CLK
  pin_epaper_cs:   "GPIO10"   # ePaper CS
  pin_epaper_dc:   "GPIO5"    # ePaper DC
  pin_epaper_rst:  "GPIO14"   # ePaper RST
  pin_epaper_busy: "GPIO6"    # ePaper BUSY
  pin_battery:     "GPIO2"    # Battery voltage ADC — must be an ADC1 pin
  pin_ldo2:        "GPIO39"   # LDO2 enable — cuts display power during sleep
  pin_maintenance: "GPIO3"    # Maintenance mode (bridge to GND = stay awake for OTA)
  pin_wakeup:      "GPIO0"    # Deep-sleep wake-up pin (BOOT button)

# ─────────────────────────────────────────────────────────────────────────────
esphome:
  name: entre-display
  friendly_name: Entre Display
  on_boot:
    - priority: 600      # LDO2 tændes tidligst muligt
      then:
        - output.turn_on: ldo2_enable
    - priority: -100     # Kør script efter alle komponenter (inkl. WiFi) er initialiseret
      then:
        - script.execute: update_and_sleep

esp32:
  board: um_feathers3
  variant: esp32s3
  framework:
    type: esp-idf

logger:
  level: DEBUG
  hardware_uart: USB_CDC
  logs:
    sensor: DEBUG
    text_sensor: DEBUG
    display: DEBUG
    wifi: DEBUG

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  fast_connect: true
  on_connect:
    - logger.log: "WiFi forbundet!"
  on_disconnect:
    - logger.log: "WiFi mistet forbindelsen"

# api: bevares til OTA-opdateringer og tidssynkronisering (time: homeassistant kræver native API)
api:
  encryption:
    key: !secret api_encryption_key

ota:
  - platform: esphome

# ---------- Globals ----------
globals:
  - id: data_received
    type: bool
    restore_value: no
    initial_value: 'false'

# ---------- HTTP Request (pull-model) ----------
# Tilføj følgende til secrets.yaml:
#   ha_template_url: "http://192.168.x.x:8123/api/template"
#   ha_authorization: "Bearer DIT_LONG_LIVED_TOKEN_HER"
http_request:
  id: http_client
  timeout: 10s
  verify_ssl: false
  buffer_size_rx: 2048
  buffer_size_tx: 2048

# ---------- JSON parsing ----------
json:

# ---------- Tid via Home Assistant ----------
time:
  - platform: homeassistant
    id: sntp_time
    timezone: Europe/Copenhagen

# ---------- Deep Sleep ----------
deep_sleep:
  id: deep_sleep_control
  sleep_duration: 10min
  wakeup_pin:
    number: ${pin_wakeup}
    inverted: true

# ---------- Script: vent på WiFi, pull data via REST, opdater display, sov ----------
# Logik:
#   1. Vent på WiFi (max 30s)
#   2. Send HTTP POST til HA og sæt data_received=true ved success
#   3. Vent til data_received=true – eller max 60s total fra start
#   4. Opdater display og sov
script:
  - id: update_and_sleep
    then:
      - deep_sleep.prevent: deep_sleep_control
      - lambda: 'id(data_received) = false;'
      - wait_until:
          condition:
            wifi.connected:
          timeout: 30s
      - if:
          condition:
            wifi.connected:
          then:
            - logger.log: "WiFi OK – henter data fra HA REST API..."
            - http_request.post:
                url: !secret ha_template_url
                request_headers:
                  Authorization: !secret ha_authorization
                  Content-Type: "application/json"
                # Henter præ-formaterede strenge fra HA template sensorer.
                # Al sprog- og formateringslogik ligger i HA_config_epaper.yaml.
                body: !lambda |-
                  return R"json({"template":"{{ {'date_str':states('sensor.display_date_str'),'time_str':states('sensor.display_time_str'),'weather_str':states('sensor.display_weather_str'),'indoor_str':states('sensor.display_indoor_str'),'vejr_note':states('input_text.display_vejr_note'),'alarm_str':states('sensor.display_alarm_str'),'cal_header':state_attr('sensor.display_lang','cal_header'),'no_events_msg':state_attr('sensor.display_lang','no_events'),'e1':states('input_text.display_event_1'),'e2':states('input_text.display_event_2'),'e3':states('input_text.display_event_3'),'e4':states('input_text.display_event_4'),'e5':states('input_text.display_event_5'),'e6':states('input_text.display_event_6'),'e7':states('input_text.display_event_7'),'e8':states('input_text.display_event_8'),'e9':states('input_text.display_event_9'),'e10':states('input_text.display_event_10')}|tojson }}"})json";
                on_response:
                  then:
                    - lambda: |-
                        if (response->status_code != 200) {
                          ESP_LOGW("ha_pull", "HTTP fejl fra HA: status %d", response->status_code);
                          return;
                        }
                        std::string body;
                        body.reserve(response->content_length > 0 ? response->content_length : 512);
                        uint8_t buf[128];
                        int n;
                        while ((n = response->read(buf, sizeof(buf))) > 0)
                          body.append(reinterpret_cast<char*>(buf), n);
                        bool ok = json::parse_json(body, [](JsonObject root) {
                          id(display_date).publish_state(root["date_str"] | "");
                          id(display_time).publish_state(root["time_str"] | "");
                          id(display_weather).publish_state(root["weather_str"] | "");
                          id(display_indoor).publish_state(root["indoor_str"] | "");
                          id(vejr_note).publish_state(root["vejr_note"] | "");
                          id(display_alarm).publish_state(root["alarm_str"] | "");
                          id(cal_header).publish_state(root["cal_header"] | "");
                          id(no_events_msg).publish_state(root["no_events_msg"] | "");
                          id(cal_event_1).publish_state(root["e1"] | "");
                          id(cal_event_2).publish_state(root["e2"] | "");
                          id(cal_event_3).publish_state(root["e3"] | "");
                          id(cal_event_4).publish_state(root["e4"] | "");
                          id(cal_event_5).publish_state(root["e5"] | "");
                          id(cal_event_6).publish_state(root["e6"] | "");
                          id(cal_event_7).publish_state(root["e7"] | "");
                          id(cal_event_8).publish_state(root["e8"] | "");
                          id(cal_event_9).publish_state(root["e9"] | "");
                          id(cal_event_10).publish_state(root["e10"] | "");
                          return true;
                        });
                        if (ok) {
                          ESP_LOGI("ha_pull", "Data modtaget og parsed OK");
                          id(data_received) = true;
                        } else {
                          ESP_LOGW("ha_pull", "JSON parse fejl");
                        }
            # HTTP er synkront – data_received er allerede sat hvis det lykkedes.
            # wait_until nedenfor håndterer edge cases og giver en klar max-grænse.
            - wait_until:
                condition:
                  lambda: 'return id(data_received);'
                timeout: 30s
            - if:
                condition:
                  lambda: 'return id(data_received);'
                then:
                  - logger.log: "✓ Data bekræftet – opdaterer display"
                else:
                  - logger.log: "✗ Ingen data fra HA (timeout/fejl) – opdaterer display med gamle værdier"
          else:
            - logger.log: "WiFi timeout – opdaterer display uden friske data"
      - component.update: epaper
      - delay: 10s        # Waveshare 4.2" bruger 4-6s på fuldt refresh
      - output.turn_off: ldo2_enable
      - if:
          condition:
            binary_sensor.is_on: maintenance_mode
          then:
            - logger.log: "Maintenance mode aktiv – deep sleep forhindret (OTA klar)"
          else:
            - deep_sleep.enter: deep_sleep_control

# ---------- Maintenance mode pin (GPIO3, active LOW – sæt til GND for at forhindre deep sleep) ----------
binary_sensor:
  - platform: gpio
    pin:
      number: ${pin_maintenance}
      mode: INPUT_PULLUP
      inverted: true
    id: maintenance_mode
    name: "Maintenance Mode"
    internal: true

# ---------- LDO2 enable (GPIO39 → 3V3_SW → display VCC) ----------
output:
  - platform: gpio
    pin: ${pin_ldo2}
    id: ldo2_enable

# ---------- Battery -------------------------------------------------------
sensor:
  - platform: adc
    pin: ${pin_battery}
    id: battery_voltage
    name: "Battery Voltage"
    update_interval: 30s
    attenuation: 12db
    samples: 16                 # Oversampling: 16 hardware readings averaged per update (mitigates ADC jitter + WiFi sag)
    filters:
      - multiply: 1.4545        # External 100kΩ/220kΩ divider compensation: (100+220)/220 = 1.4545
    unit_of_measurement: "V"
    accuracy_decimals: 2

  - platform: template
    id: battery_percent
    name: "Battery Percent"
    unit_of_measurement: "%"
    accuracy_decimals: 0
    icon: "mdi:battery"
    update_interval: 30s
    lambda: |-
      // Non-linear Li-ion discharge LUT (piecewise linear interpolation).
      // 18650 S-curve breakpoints:
      //   4.20V=100%  4.10V=90%  4.00V=80%  3.90V=70%  3.80V=60%
      //   3.70V=50%   3.60V=40%  3.50V=30%  3.40V=20%  3.30V=10%
      //   3.20V=5%    3.10V=2%   3.00V=0%
      float v = id(battery_voltage).state;
      if (isnan(v)) return {};
      if (v >= 4.20f) return 100.0f;
      if (v >= 4.10f) return 90.0f + (v - 4.10f) / 0.10f * 10.0f;
      if (v >= 4.00f) return 80.0f + (v - 4.00f) / 0.10f * 10.0f;
      if (v >= 3.90f) return 70.0f + (v - 3.90f) / 0.10f * 10.0f;
      if (v >= 3.80f) return 60.0f + (v - 3.80f) / 0.10f * 10.0f;
      if (v >= 3.70f) return 50.0f + (v - 3.70f) / 0.10f * 10.0f;
      if (v >= 3.60f) return 40.0f + (v - 3.60f) / 0.10f * 10.0f;
      if (v >= 3.50f) return 30.0f + (v - 3.50f) / 0.10f * 10.0f;
      if (v >= 3.40f) return 20.0f + (v - 3.40f) / 0.10f * 10.0f;
      if (v >= 3.30f) return 10.0f + (v - 3.30f) / 0.10f * 10.0f;
      if (v >= 3.20f) return  5.0f + (v - 3.20f) / 0.10f *  5.0f;
      if (v >= 3.10f) return  2.0f + (v - 3.10f) / 0.10f *  3.0f;
      if (v >= 3.00f) return  0.0f + (v - 3.00f) / 0.10f *  2.0f;
      return 0.0f;

# ---------- Text sensors (udfyldes via publish_state() i HTTP on_response-lambdaen) ----------
# Al formatering og sprogtilpasning sker i HA – se HA_config_epaper.yaml.
text_sensor:
  - platform: template
    id: display_date
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: display_time
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: display_weather
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: display_indoor
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: vejr_note
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: display_alarm
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_header
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: no_events_msg
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_1
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_2
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_3
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_4
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_5
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_6
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_7
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_8
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_9
    internal: true
    lambda: 'return {""};'
    update_interval: never

  - platform: template
    id: cal_event_10
    internal: true
    lambda: 'return {""};'
    update_interval: never

# ---------- Fonts ----------
font:
  - file: "fonts/Roboto-Bold.ttf"
    id: font_large
    size: 24
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÆæØøÅå0123456789 .,:;-/°%!?–"
  - file: "fonts/Roboto-Regular.ttf"
    id: font_medium
    size: 18
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÆæØøÅå0123456789 .,:;-/°%!•?–()"
  - file: "fonts/Roboto-Regular.ttf"
    id: font_small
    size: 14
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÆæØøÅå0123456789 .,:;-/°%!•?–()"

# ---------- SPI ----------
spi:
  clk_pin: ${pin_spi_sck}
  mosi_pin: ${pin_spi_mosi}

# ---------- ePaper Display ----------
display:
  - platform: waveshare_epaper
    id: epaper
    cs_pin: ${pin_epaper_cs}
    dc_pin: ${pin_epaper_dc}
    busy_pin: ${pin_epaper_busy}
    reset_pin: ${pin_epaper_rst}
    model: 4.20in
    update_interval: never
    rotation: 0°
    lambda: |-
      const int W = 400;
      const int H = 300;
      const int M = 8;  // margin

      Color BG = COLOR_OFF;  // hvid baggrund
      Color FG = COLOR_ON;   // sort tekst

      it.fill(BG);

      // ── Dato + tid (præformateret fra HA) ─────────────
      int y = M;
      std::string date_s = id(display_date).state;
      std::string time_s = id(display_time).state;
      if (!date_s.empty() && date_s != "unavailable") {
        it.print(M, y, id(font_large), FG, date_s.c_str());
        it.print(W - M, y, id(font_large), FG, TextAlign::TOP_RIGHT, time_s.c_str());
      } else {
        it.print(M, y, id(font_large), FG, "...");
      }

      y += 32;
      it.line(M, y, W - M, y, FG);
      y += 6;

      // ── Vejr (præformateret fra HA) ───────────────────
      std::string weather_s = id(display_weather).state;
      if (!weather_s.empty() && weather_s != "unavailable")
        it.print(M, y, id(font_medium), FG, weather_s.c_str());

      std::string indoor_s = id(display_indoor).state;
      if (!indoor_s.empty() && indoor_s != "unavailable")
        it.print(W - M, y, id(font_medium), FG, TextAlign::TOP_RIGHT, indoor_s.c_str());

      y += 24;

      std::string vnote = id(vejr_note).state;
      if (!vnote.empty() && vnote != "unavailable" && vnote != "unknown") {
        it.print(M, y, id(font_small), FG, vnote.c_str());
        y += 18;
      }

      y += 4;
      it.line(M, y, W - M, y, FG);
      y += 6;

      // ── Kalender ──────────────────────────────────────
      std::string hdr = id(cal_header).state;
      if (!hdr.empty() && hdr != "unavailable")
        it.print(M, y, id(font_medium), FG, hdr.c_str());
      y += 22;

      const std::string cal_events[] = {
        id(cal_event_1).state,  id(cal_event_2).state,
        id(cal_event_3).state,  id(cal_event_4).state,
        id(cal_event_5).state,  id(cal_event_6).state,
        id(cal_event_7).state,  id(cal_event_8).state,
        id(cal_event_9).state,  id(cal_event_10).state,
      };
      int vis = 0;
      for (int i = 0; i < 10 && y < H - 28; i++) {
        if (cal_events[i].empty() || cal_events[i] == "unavailable") continue;
        char linje[64];
        snprintf(linje, sizeof(linje), "• %.60s", cal_events[i].c_str());
        it.print(M, y, id(font_small), FG, linje);
        y += 17;
        vis++;
      }
      if (vis == 0) {
        std::string no_ev = id(no_events_msg).state;
        it.print(M, y, id(font_small), FG, no_ev.empty() ? "..." : no_ev.c_str());
        y += 17;
      }

      y += 4;
      it.line(M, y, W - M, y, FG);
      y += 6;

      // ── Alarm (left) + Battery % (right) ──────────────
      std::string alarm_s = id(display_alarm).state;
      if (!alarm_s.empty() && alarm_s != "unavailable")
        it.print(M, y, id(font_small), FG, alarm_s.c_str());

      if (!isnan(id(battery_percent).state)) {
        char bat_str[12];
        snprintf(bat_str, sizeof(bat_str), "Bat: %d%%", (int)id(battery_percent).state);
        it.print(W - M, y, id(font_small), FG, TextAlign::TOP_RIGHT, bat_str);
      }

      // ── Ramme ─────────────────────────────────────────
      it.rectangle(0, 0, W, H, FG);
